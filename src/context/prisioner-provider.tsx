import { ReactElement } from 'react';
import { iPrisoners } from '../interface/prisioner-interface';
export function PrisionerContextProvider({
    children,
}: {
    children: ReactElement;
}) {
    const initialState: Array<iPrisoners> = [];
    const ap;
}
